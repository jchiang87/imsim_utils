includeConfigs:
  - ${PWD}/bps/bps_qgraph_options.yaml
  - ${PWD}/bps/bps_parsl_config.yaml

isrTaskName: cpCtiIsr
extraQgraphOptions: "{isr_QgraphOptions}"

pipelineYaml: "${IMSIM_UTILS_DIR}/pipelines/cpCti.yaml"
instrument: ${INSTRUMENT_CLASS}

#clusterAlgorithm: lsst.ctrl.bps.quantum_clustering_funcs.dimension_clustering
#cluster:
#  cp_cti_isr:
#    pipetasks: cpCtiIsr
#    dimensions: detector
#    partitionDimensions: exposure
#    partitionMaxClusters: 1000

payload:
  dataset_label: ${DATASET_LABEL}
  weekly: ${WEEKLY}
  payload_modifier: ${PAYLOAD_MODIFIER}
  inCollection: u/{operator}/linearizer{payload_modifier}_{dataset_label}_{weekly}
  payloadName: cti{payload_modifier}_{dataset_label}_{weekly}
  butlerConfig: ${BUTLER_CONFIG}
  dataQuery: "instrument='${INSTRUMENT_NAME}' and exposure.observation_type='flat' and exposure.observation_reason='ptc' ${DETECTOR_SELECTION}"
