includeConfigs:
  - ${PWD}/bps/bps_qgraph_options.yaml
  - ${PWD}/bps/bps_parsl_config.yaml

isrTaskName: cpBiasIsr
extraQgraphOptions: "{isr_QgraphOptions}"

pipelineYaml: "${IMSIM_UTILS_DIR}/pipelines/cpBias.yaml#cpBiasIsr,cpBiasCombine"
instrument: ${INSTRUMENT_CLASS}

#clusterAlgorithm: lsst.ctrl.bps.quantum_clustering_funcs.dimension_clustering
#cluster:
#  cp_bias_isr:
#    pipetasks: cpBiasIsr
#    dimensions: detector
#    partitionDimensions: exposure
#    partitionMaxClusters: 1000

payload:
  dataset_label: ${DATASET_LABEL}
  weekly: ${WEEKLY}
  payload_modifier: ${PAYLOAD_MODIFIER}
  inCollection: u/{operator}/ptc{payload_modifier}_{dataset_label}_{weekly}
  payloadName: bias{payload_modifier}_{dataset_label}_{weekly}
  butlerConfig: ${BUTLER_CONFIG}
  dataQuery: "instrument='${INSTRUMENT_NAME}' and exposure.observation_type='bias' and exposure.observation_reason='bias' ${DETECTOR_SELECTION}"
